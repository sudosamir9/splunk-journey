---
course: saif-admin
theme: 09-data-inputs
lecture: 53
lecture-title: "LAB:  Discussion and Implementation of the HTTP Event Collector"
kind: video
source: "the-splunk-enterprise-certified-admin-course-2022-with-labs_transcript.txt"
tags: [course/saif-admin, theme/09-data-inputs, transcript, kind/video]
---

# Lecture 53 — LAB:  Discussion and Implementation of the HTTP Event Collector

> Raw transcript. Notes go in `02-course-saif/notes/`, not here.

So in this section we will be discussing about the Splunk STP event collector.
So this punkish dippy event collector has gained a lot of popularity in a world that has been shifting
more into the serverless and the cloud native side.
Of course, there is no need for a package installation on the client side, which means you don't need
an actual universal forwarder to forward these HTP event data to the HTP event collector because it's
using pretty much a well protocol, which is the HTTP and it's known for being scalable, secure, and
above all it's token based for convenience and easy to maintain.
Now let's discuss more about the HTTP event collector here.
So there used to be a collector.
Think of it like API, which is exposed on the Splunk side.
Now, I used to be a collector.
Normally would accept either JSON format logs or raw data.
So we have to add points in here, which is the services collector event for JSON format events and
the services collector raw endpoint for raw data.
So there should be a collector as an endpoint that lets developers send application events directly
to Splunk via HTTP or HTTPS.
So that means.
You again, You don't need a universal forwarder because it's agent list.
So a developer can use, for instance, see URL or Curl basically to actually send those type events,
you know, to the HTTP event collector.
Now let's walk you through that.
Now it's a token based either issue to P or HTTPS for authentication.
It sends the issue to P events directly without the need for an agent.
Now pay special attention in here as to be event collector cannot accept your data unless the P data
is authenticated.
This is done by having the token in the authorization header of each HTTP request.
So.
As you're sending those HP even data.
You need to provide a token which matches the same token which has been configured in here.
Otherwise there is to be event collector will drop those events and we can see that momentarily in our
simulation here.
So this is what we're going to be using.
This is the curl command that we will be using and I will break it down for you so we will understand
more on how we can basically use the Curl command to forward those events to the HTP event collector.
So let's actually.
Jump ahead and configure basically the HTTP event collector on Splunk.
So let's switch to my admin course.
It's just an app context that I've created.
So let's go to Settings.
Data inputs.
We scroll down this one.
Now.
Here says all the tokens are currently disabled.
They can be enabled in the global setting.
So first what we need to do is to enable the global settings for each type of collector.
So let's click on that.
Let's make sure that it's enabled.
We're going to go and accept JSON format.
Now, the default index, I would go for Main.
Now for the default output group.
So think of this.
Okay, Let me just show you into our diagram.
So this comes into play when you sending those ATP event data to your ATP event collector.
Now, typically in a distributed environment, maybe you want to collect those logs in here on a Splunk
endpoint, like a heavy forwarder, but also whatever you're going to be collecting from here, sending
it to an indexing tier.
And this is exactly what it means by output.
In my case, I'm using a local machine, so I'm not going to enable the WSL, so I'm going to keep it
as it is.
I'm going to use the HTTP port 80 to 88 and I'm just going to save it.
So now the global settings has been done.
I'm going to go ahead and create a new token.
So I'm going to call it admin course.
This is completely optional.
So source name override, I can do that as well.
So I'm going to call a tech admin course.
So I'm assigning the, you know this to the source description.
I'm going to leave it optional.
This is again, the same output output groups, so I'm going to leave it to none.
So let's go ahead and hit next.
I'm going to leave that to automatic and I'm going to use the main index as the allowed index.
This is the default as a last resort.
Let's review that.
So this is the input type base to be input type.
And this is the name that we have assigned.
This is the source over, right?
Of course, the allowed indexes is main and this is the default last resort.
So and this is under the app context admin course, which is this one.
So let's submit that.
And this is actually the token.
That I have generated by enabling this end point.
Now, this is in the gooey.
Let's show you guys how that would look like under the.
Configuration.
So what I'm going to do, I'm going to use actually be tool to show you how could be tool help us to
determine where we have configured those.
So let's do.
Then.
Splunk.
And then.
Beetle.
Pope.
So I could use this, you know, to help me configure to write the right command.
So be tool.
Inputs because I'm going to be looking at the inputs dot com file for all the configurations within
the HTTP event connector.
Of course, I don't need to provide the dot com extension.
I'm going to go for list here.
The stanza is http.
And then I'm going to do minus, minus debug, hit enter.
And as I can see the configurations for the HTTP stanza that we've done for the global settings.
So let me show you.
Let's go to data inputs.
I used to be a collector.
Global settings.
And I mean, those would be located under.
Here.
So.
No, no.
And this is exactly what we've configure so HTP.
So it's enabled now in here, as you can see, it's enabled.
I'm using the index main source that I've just JSON and enable SQL is set to zero because it's disabled
in here.
So let's exit and let's actually check also the settings that we have done here under the QE and see
it under the conf file.
So.
As we see it's here.
So let's just copy that.
No, no.
Ed.
So as you see.
It's enabled, it's indexed.
Main allowed indexes is also main and this is the source overwrite that we've done which is here.
And this is the token that I'll be using, basically.
And here in my core, my curl command, so I can forward the event data to the ATP event collector.
So I'm going to change this one.
So let me just remove this part.
And then copy this whole kernel command and then run it.
So if you see this as 394, this is 394.
So I'm using this token because on both ends needs to match.
So let's go to here and let's run that kernel command.
Copy.
Paste.
Of course.
Let's break it down.
So curl minus K, that's the curl command.
In case you don't have the kernel command you can do is sudo apt install kernel.
So that's the core command minus k to skip cert check.
And then this is the actual host and the default port.
And if you see here, this is the endpoint services for slash collector for slash event.
This is the endpoint services collector event which I'm going to be calling for.
And then into the header, I'm going to provide the authorization.
Always append provide Splunk as identifier prior to the token.
Now, this is the token that I'm going to be providing.
Which matches this one.
And then minus DD for the data payload.
And this is a source type of minus JSON and the events I'm going to be creating a field value pair.
So this is kind of like adjacent format.
So let's hit enter.
And as you can see, it's a success.
So let's go back.
And here.
And actually, let's go into the search.
Sorry.
Let's go to in search.
From here and let's hit index equal to.
Linux.
Was it?
No, it was main.
And here we go.
So now if you see.
I have created a field, one of value, one field, two of the value to host, which is the indexer
with the port 88 eight which is the heck which is listening on this port.
I have the source overwrite, which I've just defined under the settings and to the heck configuration
and I have the source type minus JSON, underscore JSON.
So this is exactly what I've done.
Now what I'm going to do, I'm going to change the values, maybe value to or actually value three.
Field three.
Value three.
Field three.
Let's hit enter.
Let's search again.
And here we go.
Now we have filled three.
Value three.
Field two.
You know, so pretty much this is what I've done.
And of course.
What we can do as well.
Let me change, actually.
So let me just put for.
Or let me change that token and maybe make it like instead of four, I'm going to put a five at enter
and valid token y because this token now.
Does not match the token that I configured this hack with, so they should be event collector.
So if I'm going to search.
I still don't have that.
So this one is not going to accept this HTP event payload.
So let's go back again and change that to number four.
Twist number four.
It was number four or.
Let me see.
Event.
Actually hear number four.
Now it's a success again.
So let's go back.
Let's search it.
And here we go.
Now we have that entry here.
So this is how we use the curl command.
To help developers kind of send those logs to the HDB, even collector.
Now we have been using so far the authorization header.
What we can do based on the Splunk documentation, which is here, it's a very good documentation.
We can use actually basic authentication.
So we can provide the minus here, which is the username plus the token.
So we do not need to provide the password.
It's just that the admin user and the same heck token.
So let me show you that.
So let's go back.
This is the token.
I'm going to delete basically Splunk.
Instead of authorization.
I'm just going to include Splunk as a user because I have the Splunk user and instead of minus H, I'm
going to use minus.
To success.
So let's go back and search.
So it should have four entries now instead of three.
And here we go.
This is the fourth entry.
