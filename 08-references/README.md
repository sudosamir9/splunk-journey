# 08-references

Fast-lookup reference material: cheatsheets, port lists, conf-file precedence order, default values, and other "I just need the fact" tables. Distinct from `04-concepts/` (which explains) and `03-topics/` (which synthesizes) — this folder is pure recall under the fingertips, the stuff worth pinning near the desk during labs and exams.
